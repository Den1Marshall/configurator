'use client';
import { Dialog, Paper, Stack } from '@mui/material';
import { animated } from '@react-spring/web';

export const AnimatedDialog = animated(Dialog);
export const AnimatedPaper = animated(Paper);
export const AnimatedStack = animated(Stack);
